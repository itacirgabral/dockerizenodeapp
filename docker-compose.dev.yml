version: '3.5'
services:
  web:
    image: itacirgabral/dockerizenodeapp:v0.0.1_dev
    ports:
      - "4000:4000"
    environment:
      HOST: "0.0.0.0"
      PORT: "4000"
    volumes: 
      - ./src:/app/src
    command: npm run dev